<!DOCTYPE html>
<html class="h-100 w-100 bg-primary">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Blog Symfony{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@4.5.2/dist/cerulean/bootstrap.min.css" integrity="sha384-3fdgwJw17Bi87e1QQ4fsLn4rUFqWw//KU0g8TvV6quvahISRewev6/EocKNuJmEw" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body class="h-100 w-100 position-relative d-flex flex-column">
        {% block navbar %}
            {{ include('./éléments/navbar.html.twig') }}
        {% endblock %}

        <div class="position-relative w-100 h-100">
            <div class="position-absolute top-0 left-0 w-100 d-flex flex-column justify-content-center align-items-center bg-primary" style="min-height: 100%;">
                {% for message in app.flashes('notification') %}
                    <div class="alert alert-warning">
                        {{ message }}
                    </div>
                {% endfor %}
        
                {% block body %}
                {% endblock %}
            </div>
        </div>
    </body>
</html>
